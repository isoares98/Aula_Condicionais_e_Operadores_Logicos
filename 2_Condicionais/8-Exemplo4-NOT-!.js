const temIngresso = true;

// colocar ! na frente da varivel boleando (que é true ou false) para inverter o valor
// exemplo a variavel é true, mais no if você quer que caso o valor seja false, ela entre para a proxima etapa
if (!temIngresso) {
    console.log(`Pessoa sem ingresso`);
}