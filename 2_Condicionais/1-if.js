// if (ou se em portugues), decide qual parte do condigo será executado, SE ele se enquadrar no que vc desejar

let idade = 18;

if (idade >= 18) {
    // é executado caso a condição seja verdadeira
    console.log(`é maior de idade`);
}

// caso a variavel for falso, por exemplo não é maior ou igual a 18, o codigo pula o if e não imprimi nada no terminal.

//let idade = 9;

//if (idade >= 18) {
    // é executado caso a condição seja verdadeira
 //   console.log(`é maior de idade`);}